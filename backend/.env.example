# Django Configuration
SECRET_KEY=django-insecure-change-this-in-production-esg-platform-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,localhost:8000

# Database Configuration (PostgreSQL settings)
# If these variables are omitted or blank, the application will fallback to SQLite for local development.
DB_NAME=esg_db
DB_USER=esg_user
DB_PASSWORD=esg_password
DB_HOST=localhost
DB_PORT=5432
